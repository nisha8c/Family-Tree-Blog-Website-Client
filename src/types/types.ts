export interface IAbout {
    title: string;
    description: string;
    imgUrl: string;
}

export interface IFamily {
    name: string;
    birthday: Date;
    slug: string;
    image: string;
    bio: [];
}